import { TarjetaHoverDirective } from './tarjeta-hover.directive';

describe('TarjetaHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new TarjetaHoverDirective();
    expect(directive).toBeTruthy();
  });
});
