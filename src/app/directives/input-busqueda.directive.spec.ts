import { InputBusquedaDirective } from './input-busqueda.directive';

describe('InputBusquedaDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBusquedaDirective();
    expect(directive).toBeTruthy();
  });
});
