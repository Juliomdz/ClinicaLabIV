import { ComentarioPipe } from './comentario.pipe';

describe('ComentarioPipe', () => {
  it('create an instance', () => {
    const pipe = new ComentarioPipe();
    expect(pipe).toBeTruthy();
  });
});
