import { HorarioTurnoPipe } from './horario-turno.pipe';

describe('HorarioTurnoPipe', () => {
  it('create an instance', () => {
    const pipe = new HorarioTurnoPipe();
    expect(pipe).toBeTruthy();
  });
});
